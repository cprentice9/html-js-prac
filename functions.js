function add() {
    var numOne = document.getElementById('num1').value;
    var numTwo = document.getElementById('num2').value;
    var theAnswer = parseInt(numOne) + parseInt(numTwo);
    alert(theAnswer);
    };

function subtract() {
    var numOne = document.getElementById('num1').value;
    var numTwo = document.getElementById('num2').value;
    var theAnswer = parseInt(numOne) - parseInt(numTwo);
    alert(theAnswer);
    };

function multiply() {
    var numOne = document.getElementById('num1').value;
    var numTwo = document.getElementById('num2').value;
    var theAnswer = parseInt(numOne) * parseInt(numTwo);
    alert(theAnswer);
    };

function divide() {
    var numOne = document.getElementById('num1').value;
    var numTwo = document.getElementById('num2').value;
    var theAnswer = parseInt(numOne) / parseInt(numTwo);
    alert(theAnswer);
    };